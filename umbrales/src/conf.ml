(**Archivo de configuracion que guarda los parametros de la heuristica*)

(**tamanio del lote*)
let l = 500

let bloque = 10000

(**Constante para definir las desconexiones*)
let c = 5.

(**epsilon para indicar la tempreratura de termino*)
let et = 0.00001

(**epsilon para la diferencia entre lotes*)
let ep = 0.005

(**Valor que maraca el decenso de tempreratura*)
let phi = 0.95

(**Temperatura inicial*)
let tem_inicial = 8.

let exp = "exp11"
